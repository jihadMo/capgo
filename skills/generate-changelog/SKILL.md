---
name: generate-changelog
description: Automatically generates a structured Keep-a-Changelog CHANGELOG.md from git commit history categorized by Added, Fixed, Changed, and Removed.
---

# Generate Structured CHANGELOG

Run this skill or the Python script to inspect git commit history since the last release tag and output a clean `CHANGELOG.md`.

## Execution

```bash
python3 scripts/generate-changelog.py
```
